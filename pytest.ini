[pytest]
minversion = 6.0
addopts = --strict-markers
testpaths = lob_analyzer/tests
python_files = test_*.py
markers =
    asyncio: mark async tests
    slow: mark slow tests (full pipeline, integration, etc)